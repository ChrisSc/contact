{
  "name": "contact",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:single": "tsc --noEmit && vite build --config vite.config.ts --mode singlefile",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.128.0",
    "tone": "^14.9.2"
  },
  "devDependencies": {
    "@types/three": "^0.128.0",
    "jsdom": "^28.1.0",
    "typescript": "^5.7.0",
    "vite": "^6.2.0",
    "vite-plugin-singlefile": "^2.0.0",
    "vitest": "^3.0.0"
  }
}
